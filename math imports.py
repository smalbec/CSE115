import math

a = 3.14

y = math.pi

y = round(y, 2)


b = math.pi

b = pow(b, 10)


leo = "tonto"

leo = leo.replace("tonto", "wea")

print("weon " + str(a) + " qlao " + str(y) + " la " + str(b) + " " + str(leo))

import math

import math

def distance(x,y):
    euclidian = math.sqrt(((12.5 - x)**2) + (-7.3 - y)**2)
    return euclidian


def printSequence(seq):
    for x in seq:
        print(x)

printSequence(['a', 'b', 'c'])


def explode(str):
    x = []
    for v in str:
        x.append(v)
    return x